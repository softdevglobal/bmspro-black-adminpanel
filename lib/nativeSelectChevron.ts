/**
 * Class defined in `app/globals.css` as `select.select-inset-chevron`.
 * (Tailwind-only arbitrary URLs were not reliably generating the chevron.)
 */
export const nativeSelectInsetChevronClass = "select-inset-chevron";
